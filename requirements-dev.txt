# VoidSend - Development dependencies
# Install: pip install -r requirements-dev.txt
# (Also installs production deps)

-r requirements.txt

# Linting & formatting
ruff>=0.4.0
black>=24.0.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0

# Packaging (build distributable binary)
pyinstaller>=6.0.0
